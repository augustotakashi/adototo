<p id="notice"><%= notice %></p>

<br> 
  <h1> Faça uma adoção</h1>
</br>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @carts.each do |cart| %>
      <tr>
        <td><%#= link_to 'Mostrar', cart %></td>
        <td><%#= link_to 'Editar', edit_cart_path(cart) %></td>
        <td><%#= link_to 'Deletar', cart, method: :delete, data: { confirm: 'Tem certeza?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nova Adoção', pets_path ,class: 'button is-warning add-to-cart'%>
